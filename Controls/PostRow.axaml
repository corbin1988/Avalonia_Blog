<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:Avalonia_Blog.ViewModels"
             x:Class="Avalonia_Blog.Controls.PostRow"
             x:DataType="vm:PostRowViewModel">
    <StackPanel>
        <Button Content="{Binding Title}" Command="{Binding GoToPost }" CommandParameter="{Binding PostId}" />
    
        <!-- <TextBlock Text="{Binding Title}" />
        <TextBlock Text="{Binding PostId}" /> -->
    </StackPanel>
</UserControl>